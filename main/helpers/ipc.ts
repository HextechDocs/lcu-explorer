import { ipcRenderer as ipc } from "electron";
import logger from "./logger";

// ipc.on("close", () => {
//   logger.warn("Close event triggered via close button.");
// });
//
// ipc.on("minmax", () => {
//   logger.debug("Window min/max via minmax button.");
// });
//
// ipc.on("min", () => {
//   logger.debug("Window minimized via button.");
// });

export default {};
